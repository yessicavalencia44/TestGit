git init                                    #inicializa um novo repositorio
git status                                  #verificar o status atual dos repositorios git                 
git add ""
git add .                                   #adicona os arquivos atuais ao proximo commit  
git commit -m "commit inicial"              #cria um novo commit com uma mensaje

git config --global user.email "email..."

git push                                    #envia as atualizaçoes para a nuvem na branch atualemnte ativa                                     
git remote add origin ...com
git push --set-upstream origin master #(subir codigo)

#nuevas versiones
git add .
git commit -m "....."
git push

#versiones
git reflog

#erro e voltar
fit reset -- hard numero de la version de reflog en amarillo 


BRANCH
git branch                                                    #permite listar e ver qual branch está ativa atualmente
git branch staging                                            #staging nombre cualquiera
git checkout nome-da-branch                                   #permite mudar para uma nova branch

git add .
git push 
git push --set-upstream origin staging


#trazer actualizacoes para o master
git branch
git checkout master  #nombre del branch

git merge branch-a-receber-merge          #permite fazer o merge da branch ativa atualemte com outra branch


git pull                                  #atualiza branch atualemnte ativa

git merge


# crear branch del master sin quebrar la brach principal
git checkout -b "nome-da-branch-de-origem" "nome-da-nova-branch"    #permite mudar e criar uma nova branch com base em outra


#ignore
touch .gitignore


